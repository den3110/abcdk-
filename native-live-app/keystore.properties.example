# Copy this file to keystore.properties and replace the placeholders
# with your production signing values. Paths are relative to native-live-app/.

storeFile=app/release-keystore.jks
storePassword=replace-with-store-password
keyAlias=replace-with-key-alias
keyPassword=replace-with-key-password
